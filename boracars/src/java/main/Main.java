/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package main;

import task.CLCarCrawler;
import dao.MakeModel;
import java.util.List;

/**
 *
 * @author ecolak
 */
public class Main {
    public static void main(String... args) {
        //CLCarCrawler crawler = new CLCarCrawler(0,60000,"");
        //crawler.start();
    }

    /*public static void changeToUpperCase() {
        List<MakeModel> all = MakeModel.getAll();
        for(MakeModel mm : all) {
            if(mm.getMake() != null) {
                mm.setMake(mm.getMake().toUpperCase());
            }
            if(mm.getModel() != null) {
                mm.setModel(mm.getModel().toUpperCase());
            }
        }
    }*/
}
